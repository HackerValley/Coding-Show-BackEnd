import commentHandlers from '../handlers/comment_handlers.js';

export default function (app) {
  // 留言获取
  // app.get('/api/comment/:id', (req, res) =>{});
  // 留言
  // app.post('/api/comment', (req, res) =>{});
}
